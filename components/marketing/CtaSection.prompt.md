One-line: the shared closing CTA — every marketing page ends with this, then `SiteFooter`.

```jsx
<CtaSection />
```

The rotation is the argument: three different manual chores, one replacement. Keep both CTAs and the "Live in 24 hours" claim.
