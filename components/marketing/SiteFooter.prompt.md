One-line: the marketing footer — ships with the real myDevices links and social handles already wired.

```jsx
<SiteFooter />
```

Column headings are mono uppercase at +0.1em tracking. Two columns only ("Company", "Resources") — the site deliberately keeps the footer thin.
