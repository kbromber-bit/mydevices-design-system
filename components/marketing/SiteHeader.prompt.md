One-line: the site's sticky header — use it at the top of every marketing page.

```jsx
<SiteHeader active="Use Cases" onNavigate={(i) => setPage(i.label)} />
```

Nav is five items, uppercase League Spartan 600. There is one CTA ("Book a demo") and one text link ("Sign in") — do not add more.
