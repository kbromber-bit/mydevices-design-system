One-line: shows the finished artifact the platform generated — always the payoff at the end of an AI exchange.

```jsx
<ArtifactCard kind="deck" file="Management Review — May 2026.pptx" total={10}
  images={['../../assets/artifacts/deck-slide-1.png','../../assets/artifacts/deck-slide-2.png','../../assets/artifacts/deck-slide-3.png']} index={1} />
```

Use the real filenames and real page images from `assets/artifacts/`. The `✓ Generated` badge is the only other place green appears.
