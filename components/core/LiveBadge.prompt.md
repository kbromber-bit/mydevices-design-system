One-line: the green-dot live indicator — use it to prove something is working, never as decoration.

```jsx
<LiveBadge>Live in 24 hours · no credit card</LiveBadge>
<LiveBadge variant="inline">live</LiveBadge>
```

The pill sits under hero CTAs; the inline form lives in panel chrome bars (e.g. the AI chat header). Green (#1BB98A) appears nowhere else in the system.
