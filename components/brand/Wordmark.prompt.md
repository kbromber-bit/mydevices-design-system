One-line: stands in for the myDevices logo, which was NOT supplied in the source materials — never draw or reconstruct the real mark.

```jsx
<Wordmark size={28} />
<Wordmark size={26} tone="dark" />
```

When the real `mydevices-logo.png` arrives, replace usages with `<img src="assets/logo/mydevices-logo.png" alt="myDevices" style={{height:28}} />`.
